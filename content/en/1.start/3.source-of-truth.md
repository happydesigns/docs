---
title: Source of truth
description: Which source to use for docs, code, product repositories, planning notes, and agent-facing knowledge.
navigation:
  icon: i-lucide-book-check
---
Different questions need different sources. Published docs explain strategy and architecture. Repositories show code and package metadata. `.agents/` and task logs show work in progress.

## Which source wins

| Question | Source of truth |
| --- | --- |
| What is the implementation state? | The product repository code and package metadata. |
| What is the shared architecture rule? | `docs.happydesigns.de`. |
| How does an agent act? | Published skills plus relevant docs pages. |
| What is the product-specific implementation guide? | The dedicated product docs or repository docs. |
| What is temporary task state? | `.agents/`, Business OS, or issue/PR context, not published docs. |
| What do customers read? | `help.happydesigns.de`. |

## When sources conflict

Use repository metadata for package names and code facts. Use these docs for ecosystem boundaries. Update the docs when a product decision becomes part of how the ecosystem works.

## Examples

- Repository metadata and package registries define package names; docs use those names.
- `happydesigns/brand` is the happydesigns-specific brand layer; it is not the reusable identity system.
- Business OS details belong here only when they explain ecosystem strategy.
