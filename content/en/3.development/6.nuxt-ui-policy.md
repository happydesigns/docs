---
title: Nuxt UI policy
description: Styling policy for Nuxt UI, semantic tokens, and happydesigns components.
navigation:
  icon: i-lucide-palette
---
happydesigns uses Nuxt UI as the default UI system for app shells, navigation, buttons, forms, modals, tables, badges, tabs, dropdowns, command surfaces, toast states, and token-driven styling.

## Central color policy

Do not solve custom colors by scattering `dark:` and light/dark Tailwind variants throughout components.

Define the color system centrally through Tailwind `@theme`, Nuxt UI semantic colors, `app.config.ts`, and `--ui-*` CSS variables. Components should consume semantic tokens.

## Preferred flow

```txt
raw brand palette
  ->
Nuxt UI semantic colors
  ->
CSS variables
  ->
component defaults
  ->
component usage
```

## Component policy

- Define raw brand palettes centrally in the identity or brand layer using Tailwind v4 `@theme`.
- Map palettes to Nuxt UI semantic colors in `app.config.ts`.
- Override `--ui-*` CSS variables centrally for background, text, border, radius, and container behavior.
- Use semantic utilities such as `text-default`, `text-muted`, `text-highlighted`, `bg-default`, `bg-muted`, `bg-elevated`, `border-default`, and `border-muted`.
- Prefer Nuxt UI components before raw HTML for common app surfaces.
- Use `UTable` for row and column data instead of rebuilding tables with cards or grids.
- Read generated Nuxt UI theme files when customizing slots.
- Keep local `ui` overrides small and reusable.

## Avoid by default

Avoid repeating patterns like:

```html
<div class="text-gray-900 dark:text-gray-100 bg-white dark:bg-gray-950">
```

Raw palette choices and repeated light/dark branches make reusable UI harder to maintain.

## Where raw palette classes are allowed

Raw palette classes are allowed:

- Inside the identity or brand layer.
- For logos.
- For controlled illustrations.
- For brand canvas areas.
- For intentional art direction.

Raw palette classes are not the default styling method for reusable UI.
