---
title: Nuxt UI policy
description: Styling policy for Nuxt UI, named color roles, app config, central color mapping, and happydesigns components.
navigation:
  icon: i-lucide-palette
---
happydesigns uses Nuxt UI as the default UI system for app shells, navigation, buttons, forms, modals, tables, badges, tabs, dropdowns, command bars, toast states, and token-driven styling. This policy prevents reusable components from scattering raw colors, dark-mode variants, and one-off styling decisions.

Define the color system centrally. Components use named color roles such as `primary`, `neutral`, and `error` instead of choosing raw brand colors.

## Styling flow

Follow this flow:

1. Define raw brand palettes centrally in the identity or brand layer.
2. Map those palettes to Nuxt UI color roles such as `primary`, `neutral`, and `error`.
3. Override shared `--ui-*` variables centrally.
4. Set component defaults in `app.config.ts`.
5. Use Nuxt UI props and color utilities in components.

## Component policy

- Implement the styling flow in the identity or brand layer before changing local components.
- Use Nuxt UI color utilities such as `text-default`, `text-muted`, `text-highlighted`, `bg-default`, `bg-muted`, `bg-elevated`, `border-default`, and `border-muted`.
- Prefer Nuxt UI components before raw HTML for common app UI.
- Use `UTable` for row and column data instead of rebuilding tables with cards or grids.
- Read generated Nuxt UI theme files when customizing slots.
- Keep local `ui` overrides small and reusable.

## Avoid by default

- `text-gray-900 dark:text-gray-100`
- `bg-white dark:bg-gray-950`
- Raw palette choices in reusable application components.
- Rebuilding standard Nuxt UI controls from scratch.
- Local styling fixes that belong in central token decisions.

## Raw palette classes are allowed

Inside identity layers, logos, controlled illustrations, brand canvas areas, and intentional art direction.
